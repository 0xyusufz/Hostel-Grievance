# HARDENING.md

| ID | Finding | Risk | Change | Verification | Residual Risk |
|----|---------|------|--------|--------------|---------------|
| H-01 | `src/server/app.ts:23` CORS `origin=>(origin??'*'), credentials:true` reflects any origin | High | Whitelist `FRONTEND_ORIGIN` `Set`, `origin=>ALLOWED.has(origin)?origin:null`, `secureHeaders()`, `allowMethods/Headers maxAge 600` | `curl -H Origin:evil.com /api/me` no `ACAO`, `npx tsx /tmp/cors.mjs` `ACAO!=evil.com`, `vitest CORS evil blocked` | Low |
| H-02 | `src/server/routes/grievances.ts:200` `GET /:id` only `requireUser` -> any student reads any grievance `stu-1 GET GRV-0003` | Critical | `+ assertCanViewGrievance(user,row)` imported from `queries.ts:81` | `npx tsx /tmp/exploit2.mjs` `200->403`, `vitest student cannot access others 403` | Low |
| H-03 | `src/server/routes/grievances.ts:121,135` `GET/POST /:id/comments` no owner check | High | `assertCanViewGrievance` on both + `sanitizeBody` strip `<>` + `1-1000` limit | `vitest comments IDOR 403`, `POST <script> -> stripped` | Low |
| H-04 | `src/server/routes/attachments.ts:10` `GET /attachments/:id` only `requireUser` -> `priya fetch att-1 stu-1` | High | `user=requireUser` + `grievance=requireGrievance` + `assertCanViewGrievance(user,grievance)` | `vitest stolen att 403` `200->403` | Low |
| H-05 | `src/server/routes/grievances.ts:233` `PATCH` student `row.student_id` never checked + `statusToDb` allows `Resolved` escalation | Critical | `if(row.student_id!==user.id)403` + `if(wantsStatus)403 only wardens` + keep `warden wantsContent 403` | `vitest status warden only 403`, `exploit PATCH others/title 200->403` | Low |
| H-06 | `src/server/auth/session.ts:49` `setCookie {path,maxAge}` no `httpOnly/secure/sameSite` -> `document.cookie` theft via XSS | High | `httpOnly:true secure:isProd sameSite:Lax` + `clear same attrs` | `Set-Cookie: HttpOnly; SameSite=Lax` `cors.mjs` | Low |
| H-07 | `src/server/auth/session.ts:29` `readSessionUser` ignores `expires_at` -> token forever | High | `if(new Date(expires_at)<now){destroySession;return}` | `DB UPDATE expires_at past -> /api/me 401` `vitest session expiry 401` | Low |
| H-08 | `src/server/routes/auth.ts:36` `POST /logout` only `clearCookie` never `destroySession` -> replay after logout | High | `destroySession(db,token)` on login rotation + logout | `exploit me after logout 200->401`, `vitest me after logout 401` | Low |
| H-09 | `src/server/auth/passwords.ts:3` `sha256:hex` no salt fast `703b0...` rainbow | High | `scrypt 64B +16B random hex salt` `scrypt:salt:hash` `timingSafeEqual` | `hash=scrypt:...` `DB seed` `sha256` fails closed | Low - reseed existing DB |
| H-10 | `src/server/storage/attachments.ts:35,57,39` `newStoredName(mime,orig)=>orig` + `join` no guard + `file.type` client spoof | High | `newStoredName(mime)=>random 16B hex+ext` + `write resolve guard .. / \` + `sniffMime PNG/JPEG/GIF/WEBP` check | `form ../../evil.txt ->400/201 without ../`, `existsSync ../../evil false`, magic mismatch `400` | Low |
| H-11 | `src/lib/components/app/comment-timeline.svelte:49` `{@html comment.body}` stored XSS | High | `sanitizeBody` strip `<>` on `POST comments` `grievances.ts:10` | `POST <img onerror>alert> stored without <>` | Low |
| H-12 | `src/server/http/errors.ts:25` non-`HttpError` leaks `err.message` `sqlite/stack/ENOENT` | Medium | `SyntaxError->400`, else `Internal server error` generic, log server only | `vitest 404 no sqlite/stack` | Low |
| H-13 | `src/server/routes/auth.ts:11` `POST /login` no rate-limit -> brute `student123` | Medium | `Map 5/15min` per `ip:email` `429` + dummy `verifyPassword` for enumeration | `5 wrong -> 429` | Low - in-memory not distributed |
| H-14 | `src/server/db/seed.ts:21` hardcoded `student123/warden123` + auto seed if `userCount==0` -> prod default creds | Medium | `NODE_ENV=production && !ALLOW_SEED skip` + `SEED_*_PASSWORD env` `seed.ts:9` | `NODE_ENV=production db:init skip` | Low |
| H-15 | `src/routes/+layout.ts:13` + `src/lib/stores/auth.svelte.ts:11` + `src/lib/services/api.ts:41` `localStorage hg.session.user` trust -> tamper warden | Medium | `fetch /api/me credentials:include` as truth `+layout.ts:10` `auth.svelte.ts:9` `api.ts` cache only | `localStorage warden -> redirect to /student` | Low |
| H-16 | `src/server/db/queries.ts:98` `SELECT FROM ${table}` interp + `listAll` no `LIMIT` dump DoS | Medium | `ALLOWED_TABLES` whitelist + `LIMIT 100/200` + `id` length check | `vitest limit` | Low |
| H-17 | `src/server/routes/grievances.ts:35,80,96` `POST /api/grievances`, `POST /:id/comments`, `POST /:id/attachments` no rate limit -> spam/storage exhaustion | Medium | `src/server/http/rateLimit.ts` `createRateLimiter` `Map` 5 grievances/hour/user, 20 comments/hour/user, 10 attachments/hour/user separate buckets `user.id` after `requireUser`, `60s` `unref` cleanup, `429 bad_request` before DB/file | `vitest 22 pass` `TEST A 5->429 no grievance`, `B user isolation 201`, `C 20->429 no comment`, `D isolation 201`, `E 10->429 no file/row`, `F isolation 201`, `G bucket isolation 201` | Low - process-local not distributed |
